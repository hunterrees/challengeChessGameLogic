#pragma once

#include <iostream>

using namespace std;

class ComputerPlayer {
public:
  ComputerPlayer(int difficulty) {}

  int getBestMove(uint16_t board[128]) {
    return -1;
  }
  
private:
};
